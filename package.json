{
  "name": "sdohplace-intake-api",
  "version": "0.1.0",
  "private": true,
  "description": "Submission intake API for SDOH Place contributor workflows.",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "test": "node --test",
    "lint": "node --check netlify/functions/submissions.mjs && node --check netlify/functions/spatial.mjs && node --check netlify/functions/lib/spatial.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.1133.0",
    "@aws-sdk/client-s3": "^3.1133.0",
    "@aws-sdk/s3-request-presigner": "^3.1133.0",
    "@netlify/blobs": "^9.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^23.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
